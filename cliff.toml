# git-cliff configuration for the saga_forge monorepo.
# https://git-cliff.org/docs/configuration
#
# Two gems share this one config. lib/tasks/release.rake passes per-gem
# --tag-pattern and --include-path/--exclude-path so each CHANGELOG only reflects
# its own subtree (core = everything except saga_forge-dashboard/, dashboard =
# that subtree). The version heading trims both tag prefixes (`v` and
# `saga_forge-dashboard-v`).

[changelog]
header = """
# Changelog\n
All notable changes to this project are documented in this file.\n
"""
body = """
{% if version %}\
    ## [{{ version | trim_start_matches(pat="saga_forge-dashboard-v") | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
    ## [Unreleased]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
    ### {{ group | upper_first }}
    {% for commit in commits %}
        - {% if commit.breaking %}[**breaking**] {% endif %}{{ commit.message | upper_first }}\
    {% endfor %}
{% endfor %}\n
"""
trim = true
footer = """
<!-- generated by git-cliff -->
"""

[git]
conventional_commits = true
filter_unconventional = true
split_commits = false
commit_preprocessors = [
  { pattern = '\((\w+\s)?#([0-9]+)\)', replace = "([#${2}](https://github.com/radioactive-labs/saga_forge/issues/${2}))" },
]
commit_parsers = [
  { message = "^feat", group = "Features" },
  { message = "^fix", group = "Bug Fixes" },
  { message = "^doc", group = "Documentation" },
  { message = "^perf", group = "Performance" },
  { message = "^refactor", group = "Refactoring" },
  { message = "^style", group = "Styling" },
  { message = "^test", group = "Testing" },
  { message = "^chore\\(release\\):", skip = true },
  { message = "^chore", group = "Miscellaneous Tasks" },
  { body = ".*security", group = "Security" },
]
protect_breaking_commits = false
filter_commits = false
tag_pattern = "^v[0-9]"
topo_order = false
sort_commits = "oldest"

[bump]
# Pre-1.0 semver: both gems are on 0.x. A feature bumps the minor, and a
# breaking change ALSO bumps the minor (not the major) while we're on 0.x.
# Fixes bump the patch. Revisit once a gem reaches 1.0.
features_always_bump_minor = true
breaking_always_bump_major = false
